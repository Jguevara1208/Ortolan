import { useState } from "react";

function NewProject(){

    const handleSubmit = () => {
        
    }

    return (
        <div>
            <form onSubmit={handleSubmit}>
                
            </form>
        </div>
    );
};

export default NewProject